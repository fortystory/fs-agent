## Agent skills

### Issue tracker

Issues and specs live as markdown files under `.scratch/` in this repo; one feature per directory, one file per ticket. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, label strings matching their names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
