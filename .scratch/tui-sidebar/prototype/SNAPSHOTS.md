# ticket 01 prototype snapshots —— 新几何（全高左栏 + 回合条 + 状态行）

每一张都由 `tui-sidebar-probe` 用 ratatui `TestBackend` 渲染进固定尺寸 buffer 后 dump，**不是手绘**。命令见 `README.md`；每档的行列数见 `geometry-table.md`。

| 文件 | 尺寸 | 这张在回答什么 |
| --- | --- | --- |
| `frames/40x10.txt` | 40x10 | 地板尺寸：左栏（<80）整栏隐藏，主列吃掉全部 38 列；状态行退到第 2 档（丢模型） |
| `frames/60x24.txt` | 60x24 | 60 列仍 <80：左栏隐藏，转录拿到 56 列文本；状态行第 2 档 |
| `frames/80x14.txt` | 80x14 | 左栏第一次出现，窄档 28（文字身份，没有 mark）；高度 14 恰好放得下身份 + tab + 6 字段 |
| `frames/80x24.txt` | 80x24 | 窄档 28 + 24 行：左栏底部留空，转录 17 行 |
| `frames/100x24.txt` | 100x24 | 中档 34：主列内容 63 列，状态行三段齐全（48 列刚好放得下） |
| `frames/120x24.txt` | 120x24 | 宽档 40：mark 居中（左右各 1 格 air，§2 的读法）；主列 77 列 |
| `frames/174x50.txt` | 174x50 | 很宽很高：左栏仍是 40（档位只由宽度决定），转录 42 行、回合条 42 格（12 个单位不溢出） |
| `frames/120x24-draft-3.txt` | 120x24 | 草稿 3 行：输入区 3 行，转录从 16 行降到 14 行 |
| `frames/120x24-draft-12.txt` | 120x24 | 草稿 12 行：输入区吃满 10 行上限，转录只剩 7 行 |
| `frames/120x24-variant-sidebar-42.txt` | 120x24 | 变体：左栏 42（mark + 左右各 2 格 air），代价是转录像左少 2 列 |
| `frames/120x24-variant-sidebar-40-flush.txt` | 120x24 | 变体：左栏 40、mark 顶左画（票面说的「40 无留白」）——38 列的 mark 贴在最左，剩下 2 列全留在右边 |
| `frames/100x24-variant-sidebar-mid-28.txt` | 100x24 | 变体：中档直接收到 28（面板自己需要的宽度），转录多拿 6 列 |
| `frames/120x24-variant-focus-yellow.txt` | 120x24 | 变体：焦点格用黄（现有「可以动手」语义）而不是亮品红 |
| `frames/120x24-rail-3-units.txt` | 120x24 | 回合条状态 ①：3 个单位，吸底、焦点在最新（第 3 个） |
| `frames/120x24-rail-30-units.txt` | 120x24 | 回合条状态 ②：30 个单位、转录 16 行 → 溢出，最上一格 ⋮；焦点在第 19 个（= 从最新往回数第 12 格） |
| `frames/120x24-rail-30-units-focus-12-gap.txt` | 120x24 | 回合条状态 ② 的反例：同样 30 个单位，但视口停在**第 12 个单位**（从最早那头数）——按 §4「只保留最近 N 格」这一格根本不在条上，于是整条没有一个格子是亮的 |
| `frames/120x24-tab-trace.txt` | 120x24 | tab 切到 轨迹：面板读数换成一行占位符，状态行的 上下文 % 成为唯一读数（spec §3 接受的代价） |
| `frames/120x24-tab-files.txt` | 120x24 | tab 切到 文件：与 轨迹 同一张占位页，只是选中的标签不同 |
| `frames/120x24-busy-hints.txt` | 120x24 | 忙碌态（工作中）：退出提示变成 ctrl-c 退出，比空闲态短 7 列，提示行因此又放得下 就绪 |
