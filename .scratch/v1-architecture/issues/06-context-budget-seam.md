# 上下文预算接缝、工具输出截断与丢弃的分界

Type: grilling
Status: open
Blocked by: 02

## Question

决定上下文管理的接缝。综述把它称为 agent 最硬的物理约束。

要回答：

1. **预算公式住在哪一层。** 综述明确建议**不要**手写魔法数字阈值（"如 8 万 token 就压缩"），因为"换模型后立刻失效"；应照抄 opencode 的 `limit.input − min(20_000, maxOutputTokens)`——从模型输入上限里预留出"给模型写输出"的空间，预留量不超过 20k。这个计算依赖模型元数据，所以本票被 provider 票阻塞。
2. **两个机制的分界。** 综述把它们分在两处，容易被混为一谈：
   - **工具结果截断**（综述切片 4）：单次结果上限（字符/token）+ 落盘 + 只回预览。
   - **超预算时丢弃最老的工具结果**（综述第 3 节 + 切片 4）：覆盖 90% 长任务场景的 20 行代码。
   
   谁先跑？谁拥有"当前预算"这个状态？
3. **v1 的朴素版本与后置项的分界。** 综述建议 v1 先做粗暴版（**字符数/4 估 token** + 丢最老工具结果），自动 compaction（~90% 阈值）留到"第一次真的爆窗"再做。请确认这条边界，并说明粗暴版会不会让将来的 compaction 需要返工。
4. **`AGENTS.md` 注入位置。** 综述结论表标 ✅「读 `AGENTS.md`（放**第一条 user message**，不要放 system prompt）」，理由是保住 system prompt 的缓存前缀（综述第 11 节也把"前缀稳定"标 ✅，称为"免费的 5~10 倍成本差"）。这条约束怎么落到本票的接缝上？
5. 明确**不做**：repo map / tree-sitter 符号检索、向量检索 / RAG。

答案定到接缝位置与状态归属，不要实现。
